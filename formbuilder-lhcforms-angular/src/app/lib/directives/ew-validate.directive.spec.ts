import { EwValidateDirective } from './ew-validate.directive';

describe('ValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new EwValidateDirective();
    expect(directive).toBeTruthy();
  });
});
